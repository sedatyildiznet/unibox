name: Native Windows Development Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guidance:
    runs-on: ubuntu-latest
    steps:
      - name: Development release pipeline notice
        run: |
          echo "The legacy development release pipeline is retired."
          echo "Use the versioned native release workflow after connector smoke tests pass."
