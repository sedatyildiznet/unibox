# Account model

Unibox treats connector installation and user accounts as separate layers. A connector can host zero, one or multiple remote logins depending on upstream capabilities. The desktop UI therefore identifies both the service and the specific account for every conversation, notification and connection state.
