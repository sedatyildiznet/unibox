# Multi-account behavior

Where supported by an upstream connector, Unibox allows multiple remote accounts for the same service. Account identity remains visible in conversation lists, filters and notifications so two WhatsApp, Telegram or other accounts are never silently merged into one ambiguous identity.
