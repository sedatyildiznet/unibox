# Project status

Unibox currently has a compiling Windows desktop application foundation, managed local runtime provisioning, local Matrix session management, connector registry validation and adapter-aware connector control surfaces.

A CI-built Windows executable is available from successful workflow runs for smoke testing. Public stable releases remain gated on updater signing and clean-machine WSL2 runtime acceptance testing.
